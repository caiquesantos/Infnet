#include <stdio.h>

main()
{
    int num;

    printf("Entre com um numero: ");
    scanf("%d", &num);
	if (num % 2 == 0)
		printf("\nO numero eh par\n");
	else
        printf("\nO numero eh impar\n");
}
