#include <stdio.h>

main () {
    int n1, n2;
    float media;

    printf("Entre com o valor de n1:");
    scanf("%d", &n1);
    printf("Entre com o valor de n2:");
    scanf("%d", &n2);

    media = (float)(n1 + n2) /2;
    printf("\nmedia = %.1f\n", media);

}
